import { banner } from "./Banner";

export default banner

// Essa é uma exportação + usadas nas empreas. Dessa maneira conseguimos dizer qual componente será renderizado no index atráves dos import's e export's feitos aqui.